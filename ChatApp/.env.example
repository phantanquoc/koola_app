# react-native-config — build-time native injection
LOCAL_FIRST_SQLITE=
PROD_API_URL=https://api.koola.chat/api
PROD_WS_URL=https://api.koola.chat

# DEV_HOST/DEV_PORT live in dev-config.json (gitignored).
# Run `npm run dev:sync-host` from repo root to generate.
